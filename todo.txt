1. Finish Nav2 video / implement navigation
    - Nav2 now works, but moves very slowly
    - I still feel like the skid steer messes up the odometry too much to be useful. is there any way to correct it / make use of it?
2. Design test environment in Gazebo
3. Program behavioral algo / behavior tree
    - move around and inspect objects
    - return to resting spot
    - follow me, sit down if i've been in a given spot for a while
    - "go to bed" if it's a certain time / i'm not home, "wake up" when i wake up / get back home




We launch RViz, Gazebo, & SLAM as usual to get our map 
    ros2 launch rccar_lab launch_sim.launch.py (this also launches our twist_mux node)
    ros2 launch slam_toolbox online_async_launch.py params_file:=./src/rccar_lab/config/mapper_params_online_async.yaml use_sim_time:=true

We then launch Nav2 to get our costmap ()
    ros2 launch nav2_bringup navigation_launch.py params_file:=src/rccar_lab/config/nav2_params.yaml use_sim_time:=true
    Add new map set to /global_costmap/costmap

We need to launch our teleop node (on /cmd_vel_keyboard)
    ros2 run teleop_twist_keyboard teleop_twist_keyboard --ros-args -r /cmd_vel:=/cmd_vel_keyboard -p stamped:=true
